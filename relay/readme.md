# Telegram webhook relay

Configure `TELEGRAM_BOT_TOKEN`, `WEBHOOK_SECRET_TOKEN`, `RELAY_SECRET_TOKEN`, `RELAY_URL`, `RELAY_TARGET_URL`, and `RELAY_ALLOWED_HOSTS` through environment variables.

Never place real tokens or webhook URLs in this file.
